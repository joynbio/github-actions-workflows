name: OIDC AWS Credentials Setup
description: Configure AWS Credentials for OIDC based role auth

inputs:
  account:
    required: true
    type: string
  region:
    required: true
    type: string
  role_name:
    required: false
    type: string
    default: github-actions

runs:
  using: composite
  steps:
  - name: echo hello world two times
    run: |
      echo hello world 1
      echo hello world 2
